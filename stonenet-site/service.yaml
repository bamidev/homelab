apiVersion: v1
kind: Service
metadata:
  name: stonenet-site-service
  namespace: default
spec:
  type: NodePort
  
  selector:
    app: stonenet-site
    
  ports:
  - name: http
    port: 7866 # STON
    targetPort: 8080
    nodePort: 30001
    protocol: TCP
