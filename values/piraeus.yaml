extraManifests:
  - apiVersion: piraeus.io/v1
    kind: LinstorSatelliteConfiguration
    metadata:
      name: storage-pool
    spec:
      storagePools:
        - name: pool
          zfsThinPool:
            zPool: tank 

  - apiVersion: storage.k8s.io/v1
    kind: StorageClass
    metadata:
      name: replicated-storage
    provisioner: linstor.csi.linbit.com
    allowVolumeExpansion: true
    volumeBindingMode: WaitForFirstConsumer
    parameters:
      linstor.csi.linbit.com/placementCount: "2"
      linstor.csi.linbit.com/storagePool: pool

  #- apiVersion: traefik.io/v1alpha1
  #  kind: IngressRouteTCP
  #  metadata:
  #    name: piraeus-nfs-route
  #    namespace: piraeus-datastore # Match the namespace where your Piraeus service lives
  #  spec:
  #    entryPoints:
  #      - nfs # References the entrypoint created in Step 1
  #    routes:
  #      - match: HostSNI(`*`) # Matches all TCP traffic on this port
  #        services:
  #          - name: linstor-csi-nfs # Replace with your exact Piraeus service name
  #            port: 999 # The backend port exposed by Piraeus

operator:
  securityContext:
    # The linstor-csi-node image tries to create /var/lib/kubernetes, but when /var/lib is read-only, that doesn't work.
    # Probably a bug.
    readOnlyRootFilesystem: false
