apiVersion: v1
kind: Service
metadata:
  name: nextcloud-service
  namespace: default
spec:
  type: NodePort
  
  selector:
    app: nextcloud
    
  ports:
  - name: http
    port: 8080
    targetPort: 8080  # NEXT
    nodePort: 30000
    protocol: TCP
