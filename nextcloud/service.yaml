apiVersion: v1
kind: Service
metadata:
  name: nextcloud-service
  namespace: default
spec:
  type: NodePort
  
  selector:
    app: nextcloud
    
  ports:
  - name: http
    port: 6398  # NEXT
    targetPort: 8080
    nodePort: 30000
    protocol: TCP
