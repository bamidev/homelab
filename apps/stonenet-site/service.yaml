apiVersion: v1
kind: Service
metadata:
  name: site-service
  namespace: stonenet
spec:
  type: NodePort
  
  selector:
    app: production-site
    
  ports:
  - name: http
    port: 7866 # STON
    targetPort: 8080
    nodePort: 30001
    protocol: TCP
