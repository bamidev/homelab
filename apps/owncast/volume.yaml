apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filestore
  namespace: owncast
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: replicated-storage
  resources:
    requests:
      storage: 1Gi
