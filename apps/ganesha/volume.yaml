apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-folder
  namespace: ganesha
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: replicated-storage
  resources:
    requests:
      storage: 200Gi
