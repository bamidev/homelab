apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud-route
  namespace: default

spec:
  ingressClassName: traefik-lb
  entryPoints:
  - web
  routes:
    - match: Host(`nextcloud.kubes`)
      kind: Rule
      services:
      - name: nextcloud-service
        port: 80
