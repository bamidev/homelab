apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: nextcloud
spec:
  type: NodePort
  
  selector:
    app: production
    
  ports:
  - name: http
    port: 6398  # NEXT
    targetPort: 8080
    nodePort: 30000
    protocol: TCP
