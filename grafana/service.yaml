apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: default
spec:
  type: NodePort
  
  selector:
    app: grafana
    
  ports:
  - name: http
    port: 4723 # GRAF
    targetPort: 8080
    nodePort: 30002
    protocol: TCP
