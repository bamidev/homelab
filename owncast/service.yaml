apiVersion: v1
kind: Service
metadata:
  name: owncast-service
  namespace: default
spec:
  type: NodePort
  
  selector:
    app: owncast
    
  ports:
  - name: http
    port: 6962 # OWNC
    targetPort: 8080
    nodePort: 30003
    protocol: TCP
