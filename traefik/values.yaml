ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`traefik.kubes`)
    entryPoints:
      - web

providers:
  kubernetesIngress:
    enabled: true

gateway:
  providers:
    kubernetes:
      enabled: false
